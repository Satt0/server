extends layout

block content
  div(class="main-wrapper")
    h1 Moveek
    form(action='/search' method="GET")
     input(type="text", name="search", placeholder="type movie name")
     input(type="submit", value="submit")
